# Backend environment variables
# DATABASE_URL=postgresql+psycopg://user:password@localhost:5432/finance_db
# For local development you may use SQLite (already defaulted in code):
# DATABASE_URL=sqlite:///./finance.db

# Authentication / JWT
# Strongly recommended to set a secure random secret in production
JWT_SECRET=please_change_this_in_production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
